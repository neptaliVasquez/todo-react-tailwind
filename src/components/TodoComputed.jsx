const TodoComputed = () => {
  return (
    <section className='flex justify-between rounded rounded-b-md bg-white p-4 text-gray-400'>
      <span>1 items left</span>
      <button>Clear completed</button>
    </section>
  );
};

export default TodoComputed;
